version: '3.8'

services:
  mongodb:
    image: mongo:6.0
    container_name: bugradar-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: bugradar
    volumes:
      - mongo_data:/data/db
    networks:
      - bugradar-network

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: bugradar-server
    ports:
      - "5000:5000"
    environment:
      MONGO_URI: mongodb://mongodb:27017/bugradar
      PORT: 5000
      NODE_ENV: development
    depends_on:
      - mongodb
    volumes:
      - ./server:/app
      - /app/node_modules
    networks:
      - bugradar-network
    command: npm run dev

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: bugradar-client
    ports:
      - "3000:3000"
    environment:
      REACT_APP_API_URL: http://localhost:5000/api
    depends_on:
      - server
    volumes:
      - ./client:/app
      - /app/node_modules
    networks:
      - bugradar-network
    command: npm start

volumes:
  mongo_data:

networks:
  bugradar-network:
    driver: bridge
